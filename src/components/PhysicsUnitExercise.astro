---
import { type MathCourseInfo, physicsBasicsCourse } from "../content/math-courses";
import type { MathArea, MathUnit } from "../content/math1/types";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import PhysicsExerciseSet from "./PhysicsExerciseSet.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import BreadcrumbTrail from "./ui/BreadcrumbTrail.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import ContentHero from "./ui/ContentHero.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import PageNavigation from "./ui/PageNavigation.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import StudyPage from "./ui/StudyPage.astro";

type Props = {
  area: MathArea;
  unit: MathUnit;
  course?: MathCourseInfo;
  physicsLevel?: "basics" | "physics";
};

const {
  area: _area,
  unit: _unit,
  course: _course = physicsBasicsCourse,
  physicsLevel: _physicsLevel = "basics",
} = Astro.props;
const _base = import.meta.env.BASE_URL;
const _courseHomeHref = `${_base}${_course.homePath}`;
const _areaHref = `${_courseHomeHref}${_area.key}/`;
const _unitHref = `${_areaHref}${_unit.key}/`;
---

<StudyPage
  title={`単元末演習 | ${_unit.title} | ${_course.title}`}
  description={`高校${_course.title}「${_unit.title}」の単元末演習。基礎・応用・難問を採点し、小単元ごとの弱点を確認します。`}
>
  <BreadcrumbTrail
    items={[
      { label: _course.title, href: _courseHomeHref },
      { label: _area.title, href: _areaHref },
      { label: _unit.title, href: _unitHref },
      { label: "単元末演習" },
    ]}
  />
  <ContentHero
    kicker={`${_area.title} · UNIT EXERCISE`}
    title={`${_unit.title}の単元末演習`}
    description="単元全体から問題を出し、採点後に小単元別の正答状況を表示します。数値だけでなく、向き・単位・条件の読み取りも確認します。"
    compact
  />

  <PhysicsExerciseSet
    mode="unit"
    areaKey={_area.key}
    unitKey={_unit.key}
    physicsLevel={_physicsLevel}
    courseRoute={_course.routeBase}
  />

  <PageNavigation
    previous={{ label: `← ${_unit.title}の小単元一覧`, href: _unitHref }}
    next={{ label: `${_course.title}の目次へ →`, href: _courseHomeHref }}
  />
</StudyPage>
