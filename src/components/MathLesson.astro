---
import { designPracticeSteps } from "../content/answer-input-design";
import { getLessonDiagrams } from "../content/lesson-diagrams";
import {
  getCourseCurriculumKey,
  getCourseScopeLabel,
  type MathCourseInfo,
  math1Course,
} from "../content/math-courses";
import { getFormulaDerivation } from "../content/math1/formula-registry";
import type { MathArea, MathLesson, MathUnit } from "../content/math1/types";
import { getMath3FormulaDerivation } from "../content/math3/formula-derivations";
import { getMathAFormulaDerivation } from "../content/matha/formula-derivations";
import { getMathCFormulaDerivation } from "../content/mathc/formula-derivations";
import { toPortableMathText } from "../lib/math-display";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import DiagramView from "./diagram/DiagramView.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import ExerciseSet from "./ExerciseSet.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import MiddleMathExerciseSet from "./MiddleMathExerciseSet.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import SocialExerciseSet from "./SocialExerciseSet.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import BreadcrumbTrail from "./ui/BreadcrumbTrail.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import ConceptCard from "./ui/ConceptCard.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import ContentHero from "./ui/ContentHero.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import ContentSection from "./ui/ContentSection.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import GuidedPractice from "./ui/GuidedPractice.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import PageNavigation from "./ui/PageNavigation.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import SectionHeading from "./ui/SectionHeading.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import StudyPage from "./ui/StudyPage.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import SummaryPoints from "./ui/SummaryPoints.astro";
// biome-ignore lint/correctness/noUnusedImports: Astro template usage is not detected by Biome.
import WorkedExample from "./ui/WorkedExample.astro";

type Props = {
  area: MathArea;
  unit: MathUnit;
  lesson: MathLesson;
  course?: MathCourseInfo;
};

const { area: _area, unit: _unit, lesson: _lesson, course: _course = math1Course } = Astro.props;
const _base = import.meta.env.BASE_URL;
const _courseHomeHref = `${_base}${_course.homePath}`;
const _lessonIndex = _unit.lessons.findIndex((item) => item.key === _lesson.key);
const _previousLesson = _lessonIndex > 0 ? _unit.lessons[_lessonIndex - 1] : undefined;
const _nextLesson =
  _lessonIndex < _unit.lessons.length - 1 ? _unit.lessons[_lessonIndex + 1] : undefined;
const _areaHref = `${_base}${_course.routeBase}/${_area.key}/`;
const _unitHref = `${_areaHref}${_unit.key}/`;
const _unitExerciseHref = `${_unitHref}exercise/`;
const _lessonHref = (lessonKey: string) => `${_unitHref}${lessonKey}/`;
const _middleMathCourseKey =
  _course.key === "middle-math1" ||
  _course.key === "middle-math2" ||
  _course.key === "middle-math3"
    ? _course.key
    : undefined;
const _practiceSteps = designPracticeSteps(
  _middleMathCourseKey ? "math1" : _course.key,
  _lesson.practice.steps,
);
const _portablePracticeSteps = _practiceSteps.map((step) => ({
  ...step,
  prompt: toPortableMathText(step.prompt),
  placeholder: step.placeholder ? toPortableMathText(step.placeholder) : step.placeholder,
  options: step.options?.map((option) => ({ ...option, label: toPortableMathText(option.label) })),
}));
const _lessonDiagrams = getLessonDiagrams(_course.key, _lesson.key);
const _scopeLabel = getCourseScopeLabel(_course);
const _curriculumKey = getCourseCurriculumKey(_course);
const _socialCourseKey =
  _course.key === "japanese-history" || _course.key === "world-history" || _course.key === "ethics"
    ? _course.key
    : undefined;
const _formulaDerivation =
  _socialCourseKey || _middleMathCourseKey
    ? undefined
    : _course.key === "matha"
      ? getMathAFormulaDerivation(_lesson.key)
      : _course.key === "mathc"
        ? getMathCFormulaDerivation(_lesson.key)
        : _course.key === "math3"
          ? getMath3FormulaDerivation(_lesson.key)
          : getFormulaDerivation(_lesson.key);
---

<StudyPage
  title={`${_lesson.title} | ${_unit.title} | ${_course.title}`}
  description={`${_scopeLabel}「${_lesson.title}」の教材。考え方、例題直後の練習問題、3問の確認演習、まとめで学習します。`}
  curriculumKey={_curriculumKey}
>
  <BreadcrumbTrail
    items={[
      { label: _course.title, href: _courseHomeHref },
      { label: _area.title, href: _areaHref },
      { label: _unit.title, href: _unitHref },
      { label: _lesson.title },
    ]}
  />
  <ContentHero
    kicker={`${_area.title} · ${String(_lessonIndex + 1).padStart(2, "0")}`}
    title={_lesson.title}
    description={toPortableMathText(_lesson.description)}
    compact
  />

  <ContentSection labelledby="goal-title" compact>
    <SectionHeading label="GOAL" title="このページでできるようになること" id="goal-title" />
    <SummaryPoints items={_lesson.goals.map(toPortableMathText)} />
  </ContentSection>

  <ContentSection labelledby="rule-title">
    <SectionHeading
      label="RULE"
      title="考え方・ルール"
      id="rule-title"
      description="このページで使う考え方だけに絞って確認します。"
    />
    <div class="concept-list">
      {_lesson.concepts.map((concept) => <ConceptCard {...concept} />)}
    </div>
    {
      _lessonDiagrams?.rule && (
        <div class="rule-diagram">
          <DiagramView scene={_lessonDiagrams.rule} />
        </div>
      )
    }
    {
      _formulaDerivation && (
        <div class="derivation">
          <WorkedExample
            label="DERIVATION"
            title={_formulaDerivation.title}
            introduction={_formulaDerivation.introduction}
            steps={_formulaDerivation.steps.map((step) => ({
              expression: step.expression,
              note: step.note,
            }))}
          />
        </div>
      )
    }
  </ContentSection>

  <ContentSection labelledby="example-title">
    <SectionHeading label="EXAMPLE" title={_lesson.example.title} id="example-title" />
    <WorkedExample
      problem={_lesson.example.problem}
      diagram={_lessonDiagrams?.example}
      steps={_lesson.example.steps.map((step, index) => ({
        expression: step.expression,
        note: step.note,
        diagram: _lessonDiagrams?.exampleSteps?.[index],
      }))}
    />
    <GuidedPractice
      title={_lesson.practice.title}
      problem={toPortableMathText(_lesson.practice.problem)}
      steps={_portablePracticeSteps}
      hint={toPortableMathText(_lesson.practice.hint)}
      position="例題の直後に確認"
      successMessage="すべて正解です。例題の考え方を自分で再現できています。"
      errorMessage="赤く表示された項目を例題と比べて確認してください。"
    />
  </ContentSection>

  {
    _socialCourseKey ? (
      <SocialExerciseSet
        mode="lesson"
        courseKey={_socialCourseKey}
        areaKey={_area.key}
        unitKey={_unit.key}
        lessonKey={_lesson.key}
      />
    ) : _middleMathCourseKey ? (
      <MiddleMathExerciseSet
        mode="lesson"
        courseKey={_middleMathCourseKey}
        routeBase={_course.routeBase}
        areaKey={_area.key}
        unitKey={_unit.key}
        lessonKey={_lesson.key}
      />
    ) : (
      <ExerciseSet
        mode="lesson"
        courseKey={_course.key}
        areaKey={_area.key}
        unitKey={_unit.key}
        lessonKey={_lesson.key}
      />
    )
  }

  <ContentSection labelledby="summary-title" compact>
    <SectionHeading label="SUMMARY" title="このページのポイント" id="summary-title" />
    <SummaryPoints items={_lesson.summary.map(toPortableMathText)} />
  </ContentSection>

  <PageNavigation
    previous={
      _previousLesson
        ? { label: `← ${_previousLesson.title}`, href: _lessonHref(_previousLesson.key) }
        : { label: `← ${_unit.title}の目次`, href: _unitHref }
    }
    center={{ label: `${_unit.title}の小単元一覧`, href: _unitHref }}
    next={
      _nextLesson
        ? { label: `${_nextLesson.title} →`, href: _lessonHref(_nextLesson.key) }
        : { label: "単元末演習へ →", href: _unitExerciseHref }
    }
  />
</StudyPage>

<style>
  .concept-list {
    display: grid;
    gap: 1rem;
  }

  .rule-diagram,
  .derivation {
    margin-top: 1rem;
  }
</style>
